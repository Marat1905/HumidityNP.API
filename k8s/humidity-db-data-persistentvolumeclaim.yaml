apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: humidity-db-data
  name: humidity-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi