apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: humidity-db-data
  namespace: uchaly
  labels:
    io.kompose.service: humidity-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi