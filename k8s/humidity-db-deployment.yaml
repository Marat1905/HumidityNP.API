apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: humidity-db
  name: humidity-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: humidity-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: humidity-db
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: HumidityDB
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_USER
              value: postgres
          image: postgres:17-alpine
          name: humidity-db
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data/pgdata
              name: humidity-db-data
      restartPolicy: Always
      volumes:
        - name: humidity-db-data
          persistentVolumeClaim:
            claimName: humidity-db-data